export interface IProject {
    title: string;
    description: string;
    completed: number;
    openTasks: number;
    createdAt: string;
    updatedAt: string;
    id: number;
    projectColor: string;
}